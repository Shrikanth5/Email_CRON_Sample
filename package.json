{
  "name": "cronsample",
  "version": "1.0.0",
  "description": "Scheduling the sample cron job to send emails",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "node-cron": "^3.0.0",
    "nodemailer": "^6.6.3"
  }
}
